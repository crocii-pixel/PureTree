# AI 지능형 데이터 트리 엔진 (Entity-Link Reactive Graph) 아키텍처 요약

## 1. 핵심 철학 및 패러다임 전환

기존의 전통적인 객체 지향(OOP) 방식의 무거운 트리(노드가 자식과 특수 기능을 모두 짊어지는 형태)에서 벗어나, **데이터 지향 설계(DOD) 및 관계형 DB 모델링**의 본질로 회귀했습니다.

- **구조와 데이터의 분리:** 데이터 본체(Entity)와 연결 관계(Link)를 물리적으로 완전히 분리.
- **"관계도 독립된 노드다":** Union, Proxy, Enum 같은 구조적 특성이나 인과관계, 가중치 등을 노드 내부에 하드코딩하지 않고, **관계(Link)의 속성(Type, Metrics)**으로 정의함.
- **가비지 컬렉션(GC) 회피:** 모든 객체는 C++의 고성능 `CSlotMap`에 저장되며, 파이썬 등 외부 환경에는 오직 **'고정 핸들(Stable Pointer)'**만 전달하여 메모리 부하를 제로(0)로 만듦.

## 2. 코어 아키텍처 (Base Engine)

### 2.1. CSlotMap (고성능 메모리 관리자)

### 2.2. CNode (기억 세포 / 데이터 개체)

- 순수한 데이터(`type`, `key`, `value`, `status`)만을 보유함.
- **양방향 추적:** `inLinks`(내가 결과인 관계)와 `outLinks`(내가 원인인 관계) 핸들 목록을 가져, 양방향 역추적 및 전파가 즉시 가능.

### 2.3. CLink (시냅스 / 인과 관계)

- 출발지(`from`)와 목적지(`to`) 노드를 연결.
- **SLinkMetrics 편입:** 관계를 단순한 선이 아닌 '물리적/논리적 역학 관계'로 정의.
    - `force`: 작용력/중요도/결합도
    - `distance`: 거리/지연시간/논리적 복잡도
    - `polarity`: 극성 (촉진/억제)

## 3. 핵심 메커니즘 (Key Mechanisms)

### 3.1. 동적 뷰 필터링 및 선형화 (Linearization & Views)

- **Linearize:** 트리를 DFS로 순회하여 파이썬 등에서 즉시 실행 가능한 '1차원 포인터 배열(Stream)'로 평탄화.
- **Views (투영):** `skipTypes` 매개변수를 통해 특정 관계(비활성 Union, 메타데이터 등)를 투명화. (예: `Logical View`, `Physical View`, `Structural View`)

### 3.2. 반응형 이벤트 전파 (Reactive Propagation)

- `CAccount`를 통해 노드의 상태(`value`, `status`, `impactScore`)가 변하면 연결된 노드들에게 이벤트를 즉각 발송.

### 3.3. 요약 및 맥락 집약 (Div Layer)

- **Div 노드:** 하위 노드들의 세세한 변화를 구독하고 종합(Aggregation)하여 하나의 요약된 맥락으로 상위에 보고하는 중간 관리자 층. 트리의 깊은 탐색 없이 즉각적인 상황 인지 가능.

### 3.4. 영향 평가 및 예측 (Impact Evaluation)

- 특정 노드의 변화(에너지)가 `CLink`의 `force`와 `distance` 공식에 의해 네트워크를 타고 확산됨.
- **임계점(Threshold):** 먼 곳의 노드라도 누적된 에너지가 임계점을 넘으면 새로운 이벤트를 발생시키며, 이를 통해 소프트웨어 오류 파급이나 인간의 감정 연쇄 반응을 예측함.

## 4. 응용 시나리오 및 최종 목표

### 4.1. AI 점장의 뇌 (개인화 기억 및 감정 모델)

- 단편적인 LLM의 기억을 넘어, 사용자 개인의 경험, 감정, 사물 간의 상관/인과 관계를 가중치 기반으로 영구 보관하는 **'장기 기억 신경망'**.

### 4.2. 지능형 소프트웨어 개발 맵핑 (Dev Impact Analyzer)

- 글로벌 변수, 플래그, 메서드를 노드로 등록하고 '의존/제약/호출' 관계로 연결.
- 코드 한 줄을 수정했을 때, 어떤 로직이 임계점을 넘어 붕괴(Bug)될지 예측하는 시뮬레이터 기능.
- **AI의 역할:** 코드를 파싱하여 이 복잡한 트리를 자동으로 구성("뇌 빼고 입력")하고, 개발자에게 아키텍처 조언 제공.

### 4.3. 연합 트리 (Federated Trees)

- 프로젝트 관리 트리, 코드 의존성 트리, 인프라 트리가 각각 존재하되, 서로의 Div 노드를 구독하여 이질적인 데이터 간의 거대한 인과관계를 형성.